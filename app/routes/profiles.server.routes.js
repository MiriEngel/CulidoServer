// const users = require('../../app/controllers/users.server.controller');
const profile = require('../../app/controllers/profile.server.controller');

module.exports = function(app) {
    // app.route('/api/profile')
    //     .get(profile.list)
    //     .post(users.requiresLogin, profile.create);

    //   app.route('/api/profile/getByUserId').get(profile.getByUserId);

    // app.route('/api/profile/:profileId')
    //     .get(profile.read)
    //     .put(users.requiresLogin, profile.hasAuthorization, profile.update)
    //     .delete(users.requiresLogin, profile.hasAuthorization, profile.delete);
            
    //     app.route('/api/profile/updateOrderDates').post(profile.updateOrderDates);


    //  app.param('profileId', profile.profileByID);
};